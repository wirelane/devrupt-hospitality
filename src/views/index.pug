html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title Index
    body
      ul
        each hotel, evseId in listing
          li
            a(href='/evseId/' + evseId)= hotel
