html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title Index
    body
      ul
        each chargingPoint in listing
          li
            a(href='/evseId/' + chargingPoint.evseid)= chargingPoint.hotel.title + " (" + chargingPoint.evseid + ")"
